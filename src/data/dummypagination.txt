

const Showitem = 10;

const [current, setCurrent] = useState([]);
const [count, setCount] = useState(0);

useEffect(() => {
  if(countrys && countrys.length > 0){
    setCount(Math.ceil(countrys.length / Showitem));
    setCurrent(countrys.slice(0, Showitem));
  }
}, [countrys]);

const handlePageClick = event => {
  const newOffset = event.selected * Showitem;
  setCurrent(countrys.slice(newOffset, newOffset + Showitem));
};

{current.length > 0 ? (
  current.map((country, index) => (
    <p key={index}>{country.name.common}</p>
  ))
)}
