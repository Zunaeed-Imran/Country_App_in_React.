

const Showitem = 10;

const [current, setCurrent] = useState([]);
const [count, setCount] = useState(0);

useEffect(() => {
  if (countrys && countrys.length > 0){
    setCount(Math.ceil(countrys.length / Showitem));
    setCurrentItems(countrys.slice(0, Showitem));
  }
}, [countrys]);